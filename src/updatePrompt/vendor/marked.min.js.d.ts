declare const markedModule: {
  parse: (markdown: string, options?: Record<string, unknown>) => string;
  setOptions?: (options: Record<string, unknown>) => void;
  marked?: unknown;
};

export = markedModule;

